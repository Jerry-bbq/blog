{
  "name": "blog",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "publish": "pnpm run docs:build && git add . && git commit -m 'chore: build' && git push"
  },
  "dependencies": {
    "markdown-it-task-list-plus": "^2.1.3",
    "medium-zoom": "^1.1.0"
  },
  "engines": {
    "node": ">=v18.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.3.4"
  }
}